name: Deploy Service Infra

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy service
        run: echo "Deploying service B from branch ${{ github.ref }}"
